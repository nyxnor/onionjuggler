shell=sh
#disable=SC1090  # (can't follow source): source path is a variable
#disable=SC2086  # (double quote vars): some variables need to expand, remove this for verification
#disable=SC2034  # (unused vars): only for the .onionrc because the variables are not used there
#disable=SC2119  # (Use restarting_tor "$@" if function's $1 should mean script's $1) almost never happen
#disable=SC2236  # (-n instead of ! -z): does not consider the variable is not an integer
